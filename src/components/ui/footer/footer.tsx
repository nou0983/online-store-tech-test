import styles from "./footer.module.scss";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p>the fake store copyright &copy;2024</p>
    </footer>
  );
};

export default Footer;
